#include <stdio.h>

int main()
{
    int size,pos;
    scanf("%d %d",&size,&pos);
    int arr[size];
    for(int i=0;i<size;i++)
    {
    scanf("%d",&arr[i]);
    }
    for(int i=pos-1;i<size;i++)
{
    arr[i]=arr[i+1];
} 
for(int i=0;i<size-1;i++)
{
    printf("%d",arr[i]);
}
    return 0;
}
