#include <stdio.h>

int main()
{
    int size,i,temp=0;
    scanf("%d",&size);
    int arr[size];
    for(i=0;i<size;i++)
    scanf("%d",&arr[i]);
    for(i=0;i<size;i++)
    {  int t1=arr[i];
        int t2=arr[i+1];
        if(t1==t2){
    printf("%d",t1);
        }
         }
    return 0;
}
